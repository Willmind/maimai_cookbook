# 任务清单：recipe-form-field-limits-v1

## 1. Spec 与设计

- [x] 新增 proposal / design / tasks / spec delta（中文）。

## 2. 实现

- [x] 新增菜谱表单字段长度共享常量。
- [x] 在新建菜谱页面添加各字段 `maxlength`。
- [x] 在编辑菜谱页面添加各字段 `maxlength`。
- [x] 补充测试覆盖关键字段限制。

## 3. 验证

- [x] 运行 `openspec validate --all`。
- [x] 运行单元测试并通过。
- [x] 运行构建并通过。
