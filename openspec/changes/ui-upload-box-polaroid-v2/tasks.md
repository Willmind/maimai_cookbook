# Tasks: UI Upload Box Polaroid v2 (Minimal)

## 1. Spec and docs

- [x] Document minimal polaroid upload interaction.

## 2. Implementation

- [x] Update `SingleImageUpload.vue` to a single-frame + plus design.
- [x] Update `src/styles/base.css` polaroid v2 styles.

## 3. Verification

- [x] Update tests to assert behavior (events) rather than copy.
- [x] Run unit tests: `npm test`.
- [x] Run build: `npm run build`.

