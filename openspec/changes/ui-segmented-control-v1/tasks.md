# Tasks: UI Segmented Control v1

## 1. Spec and docs

- [x] Define segmented control usage and interaction states.
- [x] Confirm it supports mobile wrapping.

## 2. Implementation

- [x] Add `SegmentedControl` component with `v-model`.
- [x] Add global styles for segmented control (hover/active/selected/focus-visible).
- [x] Replace cooking result `<select>` with segmented control in `NewCookingLogView`.

## 3. Verification

- [x] Add/adjust tests for selecting cooking result.
- [x] Run unit tests: `npm test`.
- [x] Run build: `npm run build`.

