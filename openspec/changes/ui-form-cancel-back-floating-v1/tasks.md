# 任务清单：ui-form-cancel-back-floating-v1

## 1. Spec 与设计

- [x] 新增 proposal / design / tasks / spec delta（中文）。

## 2. 实现

- [x] 在表单页底部增加“取消”按钮（置于“保存”左侧）并实现返回逻辑：
  - `NewRecipeView`
  - `EditRecipeView`
  - `NewCookingLogView`
  - `EditCookingLogView`

## 3. 验证

- [x] 运行 `openspec validate --all`。
- [x] 运行单元测试（`npm test`）并通过。
- [x] 检查 lints 无报错。

