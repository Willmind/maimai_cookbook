# 设计说明：ui-form-cancel-back-floating-v1

## 设计目标
1. 表单页提供显式“取消”操作，便于用户快速返回上一页。

## 交互细节

### 1) 表单页底部：取消 + 保存
- 在底部按钮区域渲染两枚按钮：左侧“取消”、右侧“保存”。
- `取消` 点击行为：
  - 优先调用 `router.back()` 返回上一页
  - 若没有历史记录（例如页面被直接打开），则回退到合理的列表/详情路由（如 `/recipes` 或 `/recipes/:id`）

