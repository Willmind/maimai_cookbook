# 任务清单：ui-home-card-fixed-thumb-v1

## 1. Spec 与设计

- [x] 新增 proposal / design / tasks / spec delta（中文）。

## 2. 实现

- [x] 在 `HomeView` 为相关卡片列表容器添加 `home-fixed-thumb` 类。
- [x] 在 `cards.css` 内实现固定缩略图与文本截断规则。

## 3. 验证

- [x] 运行单元测试并通过。
- [x] 运行构建并通过。
- [x] 运行 `openspec validate --all`。
