# Tasks: UI Status Tokens v1

## 1. Spec and docs

- [x] Define status token names and mapping.
- [x] Document initial adoption targets.

## 2. Implementation

- [x] Add `neutral/selected/success/warning/danger/info` tokens to `src/styles/base.css :root`.
- [x] Update selected state styles to use tokens (`tag`, `filter-chip.active`, `segmented-item.selected`).
- [x] Update error state style to use tokens (`error-note`).

## 3. Verification

- [x] Run unit tests: `npm test`.
- [x] Run build: `npm run build`.

