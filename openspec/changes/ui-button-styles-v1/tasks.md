# Tasks: UI Button Styles v1

## 1. Spec and docs

- [x] Confirm scope uses existing class names (no `.btn` API).
- [x] Document button interaction states for primary/secondary/ghost/small.

## 2. Implementation

- [x] Update `src/styles/base.css` to unify hover/active/focus-visible/disabled.
- [x] Ensure top-right actions in `src/App.vue` get hover feedback via global styles.
- [x] Respect `prefers-reduced-motion`.

## 3. Verification

- [x] Run unit tests: `npm test`.
- [x] Run build: `npm run build`.

