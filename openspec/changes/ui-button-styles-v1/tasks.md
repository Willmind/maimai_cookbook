# Tasks: UI Button Styles v1

## 1. Spec and docs

- [ ] Confirm scope uses existing class names (no `.btn` API).
- [ ] Document button interaction states for primary/secondary/ghost/small.

## 2. Implementation

- [ ] Update `src/styles/base.css` to unify hover/active/focus-visible/disabled.
- [ ] Ensure top-right actions in `src/App.vue` get hover feedback via global styles.
- [ ] Respect `prefers-reduced-motion`.

## 3. Verification

- [ ] Run unit tests: `npm test`.
- [ ] Run build: `npm run build`.

