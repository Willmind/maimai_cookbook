# Tasks: UI All Recipes Card Minimal v1

## 1. Spec and docs

- [x] Document minimal card display for all recipes page.

## 2. Implementation

- [x] Update `AllRecipesView.vue` card content to title + description + thumbnail only.
- [x] Remove tag/status blocks from all recipes cards.

## 3. Verification

- [x] Update tests for simplified card content.
- [x] Run unit tests: `npm test -- --run`.
- [x] Run build: `npm run build`.
- [x] Run OpenSpec validation: `openspec validate --all`.
