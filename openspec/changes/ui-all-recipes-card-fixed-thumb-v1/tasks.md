# 任务清单：ui-all-recipes-card-fixed-thumb-v1

## 1. Spec 与设计

- [x] 新增 proposal / design / tasks / spec delta（中文）。

## 2. 实现

- [x] 全部菜谱卡片使用固定缩略图尺寸。
- [x] 标题单行省略，简介双行省略。
- [x] 样式仅作用于 `recipe-directory` 作用域。

## 3. 验证

- [x] 运行单元测试并通过。
- [x] 运行构建并通过。
- [x] 运行 `openspec validate --all`。
